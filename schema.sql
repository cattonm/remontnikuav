-- УВАГА: цей файл більше не є джерелом правди про схему.
--
-- Схема тепер живе в migrations/ і накочується рушієм:
--     python migrate.py status    # що накочено, що чекає
--     python migrate.py up        # накотити нове
--
-- Вміст, який раніше був тут, перенесено один-в-один у
-- migrations/0001_baseline.sql. Файл лишений, щоб не ламати старі
-- посилання й інструкції; нових змін сюди НЕ додавати.
